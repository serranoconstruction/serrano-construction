import HeroImage from "../images/hero.jpg";

export const Hero = () => {
  return (
    <div
      className="h-screen bg-cover"
      style={{
        backgroundImage: `url(${HeroImage.src})`,
      }}
    >
      adsf
    </div>
  );
};
